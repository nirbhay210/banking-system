from .accounts import Account
from .errors import InsufficientFunds, AccountNotFound


def deposit(accounts: list[Account], account_id: int, amount: float) -> None:
    print("[TODO] deposit")

def withdraw(accounts: list[Account], account_id: int, amount: float) -> None:
    print["[TODO] withdraw"]

def transfer(accounts: list[Account], account_id: int, amount: float) -> None:
    print["[TODO] withdraw"]
